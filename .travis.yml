language: go
go_import_path: github.com/renaynay/go-hobbits
sudo: false
env:
  - GOPATH=/home/travis/gopath

os: osx
dist: xenial
sudo: required
go: 1.12.x

jobs:
  include:
    - script:
      - go lint ./...
      - go vet ./...
      - go test ./...